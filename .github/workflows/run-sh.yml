name: Run Script

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run shell script
        run: bash ./run.sh
